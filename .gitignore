# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# If you find yourself ignoring temporary files generated by your text editor
# or operating system, you probably want to add a global ignore instead:
#   git config --global core.excludesfile '~/.gitignore_global'

Gemfile.lock

# Ignore bundler config.
/.bundle

# 忽略默认的 SQLite 数据库.
/db/*.sqlite3
/db/*.sqlite3-journal

#数据库配置信息不加入版本库(因为包含数据库密码信息),服务器上手工创建此文件
/config/database.yml

/config/school_config.yml


# 忽略所有 logfiles 和 tempfiles.
/log/*.log
/tmp
nohup.out

.ruby-version
.ruby-gemset

#忽略 rubymine ide配置信息
/.idea

tags
.swp

/public/assets/
/public/uploads/
/public/assets/*.*
/public/uploads/*

/config/schedule.rb  #whenever使用的配置文件
.keep

# 一些其它不需要的文件.
doc/
*.swp
*~
.project
.DS_Store
dump.rdb
.env
#忽略 Vagrant 配置信息和目录

spec/reports/
coverage/
.vagrant/
Vagrantfile